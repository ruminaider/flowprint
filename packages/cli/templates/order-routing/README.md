# Order Routing

An order routing workflow that classifies incoming orders using a rules-based decision table and routes them to express, standard, or manual review fulfillment paths. The `rules/routing.rules.yaml` file defines routing logic based on order amount and customer tier using a first-hit policy. Use this template for e-commerce order processing, support ticket routing, or any scenario where incoming items need rule-driven classification before handling.
