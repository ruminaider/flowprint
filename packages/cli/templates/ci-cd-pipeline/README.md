# CI/CD Pipeline

Continuous integration and deployment pipeline with parallel test execution. Models the flow from code push through build, concurrent test suites (unit, integration, E2E), quality gate evaluation, and deployment or rejection. The quality gate rules file defines pass/fail thresholds for coverage percentage, test pass rate, and lint error counts.
