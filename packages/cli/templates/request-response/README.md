# Request-Response

A straightforward request/response pattern with distinct receive, process, and respond steps split across an API gateway lane and a backend service lane. This template is ideal for modeling simple synchronous API endpoints or RPC-style service interactions. Extend it by adding error handling, validation logic, or additional processing steps.
