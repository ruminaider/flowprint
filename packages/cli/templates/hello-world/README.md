# Hello World

The simplest possible Flowprint blueprint. It contains two lanes (frontstage and backstage) with a single action node that transitions to a terminal node. Use this template as a starting point when learning the blueprint format or when you need a minimal skeleton to build upon.
